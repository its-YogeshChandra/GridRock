use tokio::{self, main};

//helper function
fn parse_env_arguments() {
    //take the arguments from the env file and parse them
}

#[main]
async fn main() {
    parse_env_arguments();
    println!("Hello, world!");
}
